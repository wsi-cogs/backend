{% extends 'template.jinja2' %}
{% block title %}Dashboard{% endblock %}
{% block content %}
    <h4>Welcome, {{ user_name }}</h4>

    {% if create_projects == "True" %}
        <h4>My owned projects
        {% if can_edit %}
            <a href="/create_project" class="btn btn-primary btn-small">Create Project</a>
        {% endif %}
        </h4>
        {% include 'group_list_overview_inline.jinja2' %}
    {% endif %}

    {% if modify_project_groups == "True" %}
        {% with create_button = "Modify rotation" %}
            {% include "group_form_inline.jinja2" %}
        {% endwith %}
    {% endif %}

{% endblock %}

{% block script %}
    {% if modify_project_groups == "True" %}
        <script>
            no_datepickers = {{ deadlines | count }};
        </script>
        <script src="/static/js/group_create.js"></script>
        <script>
            $(document).ready(function() {
                selectDate(moment().format("DD-MM-YYYY"), 0, add_days=0);
            });
            $('button[name="add_group"]').on('click', function(e) {
                e.preventDefault();
                $.ajax({
                    url: "/modify_rotation",
                    type: "post",
                    data: $("#edit-form").serialize(),
                    success: function(response) {
                        window.location.replace(response);
                    }
                });
            });
        </script>
    {% endif %}
{% endblock %}