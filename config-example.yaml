webserver:
  host: 127.0.0.1
  port: 5000

  # FIXME This doesn't appear to be used anywhere in the code. Remove,
  # or document if it is needed...
  cookie_key: abc123

  # The URL from which the CoGS application will be accessible
  service: http://cogs.example.com

database:
  # PostgreSQL credentials for CoGS data
  host: cogs_db.example.com
  port: 5432
  user: cogs
  passwd: cogs_password
  name: cogs

pagesmith_auth:
  # Passphrase for decrypting Pagesmith data
  passphrase: pagesmith_passphrase

  database:
    # MySQL credentials for Pagesmith DB
    host: pagesmith_db.example.com
    port: 3306
    user: pagesmith
    passwd: pagesmith_password
    db: pagesmith

email:
  sender: gradoffice@example.com
  smtp:
    # SMTP server details
    host: smtp.example.com
    port: 25

general:
  logging_level: DEBUG


# FIXME These things should probably be removed from the configuration

deadlines:
  supervisor_submit:
    name: Submission deadline for supervisors
    pester: [create_projects, create_project_groups]
    predicate: have_uploaded_project
    pester_template: supervisor_invite_{group_part}
  student_invite:
    name: Date students get invited
    pester_content: make sure there are enough projects for students.
    pester: [create_project_groups]
  student_choice:
    name: Deadline for student choices
    pester: [join_projects, create_project_groups]
  student_complete:
    name: Deadline for report submission
    pester: [join_projects, create_project_groups]
  marking_complete:
    name: Deadline for report feedback
    pester_content: submit feedback for the project you're marking
    pester: []

misc:
  timezone: Europe/London
  submission_grace_time: 3
  mark_late_time: 14
  job_hazard_form: "new_starter_health_questionnaire_jun_17.docx"
  max_export_line_length: 30
  max_filesize: 31457280
